# Code archive

This directory contains superseded source retained for provenance. Archived code is not
part of the current analysis, receives no compatibility guarantee, and may reference old
paths or data vintages.

Use Git history and the safety tag `pre-cleanup-2026-09-06` when reconstructing its original
context. Current code and interpretation live under `analysis/` and `docs/`.
